# 前端代码审核规则（精简版）

## 一、审核执行流程

1. 审核时若存在上下文（包括业务与代码），必须结合上下文进行综合判定。
2. 扫描规则命中项（先阻塞后告警）。
3. 审核报告固定三段输出（每次都必须包含）：
   - 明确错误：可复现、可确认的问题（对应阻塞项）；
   - 高风险疑似：暂无法完全确认但风险较高的问题（对应告警项）；
   - 建议优化：不影响本次提交但建议后续优化的项。
4. 禁止在审核阶段自动改代码，只能报告问题。

## 二、阻塞提交（必须修复）

### CR-B001 枚举值硬编码（公司/部门）
- **规则**：禁止硬编码公司枚举、部门枚举 code 值。
- **检查**：
  - 是否直接写死枚举 code；
  - 是否可通过枚举常量或配置替代。
- **处置**：发现即阻塞提交。

### CR-B002 金额计算未使用 big.js
- **规则**：金额相关计算必须使用 `big.js`（来自 `flash-tool`）。
- **检查**：
  - 是否使用 `+`、`-`、`*`、`/` 直接计算金额；
  - 是否正确引入并使用 `big.js`。
- **处置**：发现即阻塞提交。

### CR-B003 深度选择器使用 /deep/ 或 >>>
- **规则**：禁用 `/deep/`、`>>>`，统一使用 `::v-deep`（Vue 3 使用 `:deep()`）。
- **检查**：
  - 样式中是否存在 `/deep/` 或 `>>>`。
- **处置**：发现即阻塞提交。

### CR-B004 硬编码中文文案（未国际化）
- **规则**：展示文案不得硬编码中文，需使用 `$t(翻译key)`。
- **检查**：
  - 模板或脚本中是否存在非注释中文展示文案；
  - 是否已有对应翻译 key。
- **处置**：发现即阻塞提交。

### CR-B005 关联字段联动遗漏1
- **规则**：字段清空或变更后，需同步处理依赖字段与联动逻辑。
- **检查**：
  - 是否遗漏级联清空、校验重置、查询条件联动。
- **处置**：发现即阻塞提交。

### CR-B006 关联字段联动遗漏2 这点极其重要 非常重要 要仔细检验!!!
- **规则**：v-if/v-show控制的显隐字段,如果在初始化时或者关联字段联动导致的该字段隐藏(即v-if/v-show为false)的时候,该字段的值如果不是空值,则需要阻塞提交
- **检查**：
  - 是否遗漏该字段是空值的处理。
- **处置**：发现即阻塞提交。

## 三、告警项（建议修复）

### CR-W001 枚举值使用方式可优化
- **规则**：建议使用统一枚举源与常量，减少散落式判断。
- **检查**：
  - 是否存在可抽离的重复枚举判断逻辑。
- **处置**：记录告警，可继续提交。

## 四、通用语义一致性检查（兜底规则）

### CR-G001 模板绑定语义一致性
- **规则**：当属性值语义为变量、表达式、布尔、数字、对象或数组时，应使用动态绑定语法（如 `v-bind` / `:`）。
- **检查**：
  - 是否存在“值看起来是变量，但写成静态字符串”的情况；
  - 若为字面量字符串，是否明确属于业务预期。
- **处置**：默认记录告警；一旦导致功能错误（如参数类型错误、值未生效），升级为阻塞项。

### CR-G002 指令与事件语义一致性
- **规则**：事件绑定、双向绑定、条件渲染必须与业务意图一致，不得出现写法合法但语义错误的实现。
- **检查**：
  - 事件是否触发了正确方法与参数；
  - `v-model`、`v-if/v-show`、权限显示条件是否与预期行为一致。
- **处置**：默认记录告警；一旦影响核心流程，升级为阻塞项。

### CR-G003 数据流语义一致性
- **规则**：展示层、状态层、请求层的同名字段语义必须一致，避免“名称相同但含义不同”导致误用。
- **检查**：
  - 查询参数、表单字段、接口参数是否存在错位映射；
  - 是否出现状态值类型不匹配导致的隐式转换问题。
- **处置**：默认记录告警；一旦影响提交数据正确性，升级为阻塞项。

### CR-G004 高风险疑似问题强制报告
- **规则**：对无法完全确认但风险较高的问题，不允许忽略，必须输出疑似问题与建议修复方案。
- **检查**：
  - 是否存在“语法可运行但行为可疑”的片段；
  - 是否明确给出“问题描述 + 影响范围 + 建议改法”。
- **处置**：记录告警，并要求在审核结果中单独列出。

## 五、规则扩展模板（新增规则时复制）

### CR-[B/W][编号] 规则标题
- **规则**：一句话说明禁止或要求。
- **检查**：
  - 检查点 1
  - 检查点 2
- **处置**：阻塞提交 / 记录告警
